<script setup lang="ts">
import { ref } from 'vue';
import AuthService from "@/services/AuthService";
import Auth from '@/types/auth/Auth';
import AuthResponse from '@/types/auth/AuthResponse';
import { useAuthStore } from '@/store/authStore';
import { storeToRefs } from 'pinia';

const User = ref('');
</script>

<template>
    <form class="p-24 ">
        <div class="mb-6">
            <label for="email" class="block mb-2 text-sm font-medium text-gray-900">Ton pseudo</label>
            <input type="email" id="email" v-model="User"
                class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 "
                placeholder="Kathy..." required>
        </div>
        <div class="mb-6">
            <label for="password" class="block mb-2 text-sm font-medium text-gray-900">Ton mot de
                passe</label>
            <input type="password" id="password"
                class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
                placeholder="M0nSup3rbeP@ssw0rd" required>
        </div>
        <button type="submit"
            class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm w-full sm:w-auto px-5 py-2.5 text-center">S'inscrire</button>
    </form>

</template>